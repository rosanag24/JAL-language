int alohomora f(int x, int y){
    int z;
    if(x > 0){
        int z = x + y;
    }
    else{
        int z = y;
    }
    reditus(z);
}
aberto:
{
    int z = f(1, 2);
    aparecium(z);
    reditus(0);
}